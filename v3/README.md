# Member Register V3

V3 workspace for Cornerstone Church Hua Hin.

- `index.html` is the V3 frontend baseline. It displays the accepted V2 frontend unchanged so the layout can be reviewed while V3 wiring is built.
- `apps-script/Code-V3.gs` is the current V3 Apps Script backend source.
- `V3-SPEC.md` contains the V3 implementation specification when included.
- Existing V2 files remain untouched.
